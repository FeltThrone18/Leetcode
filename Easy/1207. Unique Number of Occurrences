class Solution:
    def uniqe(arr):
        lst = []
        num = set(arr)
        for i in num:
            cnt =  arr.count(i)
            if cnt in lst:
                return False
            lst.append(cnt)
        return True

    
